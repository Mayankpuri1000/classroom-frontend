export const DEPARTMENTS = ['Computer Science', 'Mathematics', 'English', 'Economics']

export const DEPARTMENTS_OPTIONS = DEPARTMENTS.map((dept) => (
    {
        value: dept, 
        label: dept
    }
))